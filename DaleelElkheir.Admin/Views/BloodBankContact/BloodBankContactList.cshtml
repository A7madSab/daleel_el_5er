@model IEnumerable<DaleelElkheir.DAL.Domain.BloodBankContact>

@{
    ViewBag.Title = "BloodBankContactList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Blood Bank Contact</h2>
<br/>
<p>
    @Html.ActionLink("Create New", "CreateBloodBankContact", null,new {@class="btn btn-primary"})
</p>
<table class="table-striped table-hover table-condensed table-bordered table" id="BloodBankContactTable">
    <thead>
        <tr>

            <th>
                @Html.DisplayName("Contact Name")
            </th>
            <th>
                @Html.DisplayName("Contact Number")
            </th> 
            <th>
                @Html.DisplayName("Blood Bank")
            </th>          
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
               
                <td>
                    @Html.DisplayFor(modelItem => item.ContactName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ContactNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BloodBank.NameEn)
                </td>         
                <td>
                    @Html.ActionLink("Edit", "UpdateBloodBankContact", new { BankContactID = item.ID }) |
                    @Html.ActionLink("Delete", "DeleteBloodBankContact", new { BankContactID = item.ID })
                </td>
            </tr>
        }
    </tbody>
</table>
@section Scripts {
    <script>
$(document).ready(function() {
    $('#BloodBankContactTable').DataTable();
});
    </script>
}